% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artcatr.R
\name{artcatr}
\alias{artcatr}
\title{Sample size / power for a randomized trial with an ordered categorical
outcome, analysed by the proportional-odds model}
\usage{
artcatr(
  pc,
  cumulative = FALSE,
  pe = NULL,
  or = NULL,
  rr = NULL,
  margin = 1,
  favourable = NULL,
  power = NULL,
  n = NULL,
  aratio = c(1, 1),
  alpha = 0.05,
  onesided = FALSE,
  method = c("ologit", "whitehead"),
  variance = c("NA", "NN", "AA"),
  quiet = FALSE
)
}
\arguments{
\item{pc}{Numeric vector. Anticipated probabilities (or cumulative
probabilities, if \code{cumulative = TRUE}) in the control arm, one per
outcome level, including the right-most level (probabilities must sum
to 1, or cumulative probabilities must reach 1).}

\item{cumulative}{Logical. Are \code{pc}/\code{pe} cumulative probabilities?}

\item{pe}{Numeric vector. Anticipated (cumulative) probabilities in the
experimental arm, specified like \code{pc}. Exactly one of \code{pe}, \code{or}, \code{rr}
must be given (or none, if \code{margin != 1}, in which case \code{or = 1} is
assumed).}

\item{or}{Numeric scalar. Anticipated common odds ratio applied to the
control-arm cumulative probabilities (assumes proportional odds).
An odds ratio below 1 shifts probability towards the right-most level.}

\item{rr}{Numeric scalar. Anticipated common risk ratio (applied to every
level except the right-most).}

\item{margin}{Numeric scalar odds ratio defining the null hypothesis.
\code{margin = 1} (default) gives a superiority trial; otherwise a
non-inferiority or substantial-superiority trial (see \code{favourable}).}

\item{favourable}{Logical or \code{NULL}. Is the left-most outcome level the
most favourable outcome? If \code{NULL} (default), inferred from the
anticipated average odds ratio vs \code{margin}.}

\item{power}{Numeric scalar in (0, 1). Power to achieve; sample size is
calculated. Default \code{0.8} if neither \code{power} nor \code{n} given.}

\item{n}{Numeric scalar. Total sample size; power is calculated.}

\item{aratio}{Length-2 numeric vector, allocation ratio control:experimental
(e.g. \code{c(1, 2)} means 2 experimental participants per 1 control).}

\item{alpha}{Numeric scalar significance level. Default \code{0.05}.}

\item{onesided}{Logical. Is \code{alpha} one-sided? Default \code{FALSE} (two-sided).}

\item{method}{One of \code{"ologit"} (default, the new method) or \code{"whitehead"}.}

\item{variance}{For \code{method = "ologit"}, one of \code{"NA"} (default, recommended
by White et al 2023), \code{"NN"} (equivalent to Whitehead's method), or
\code{"AA"}. \code{"NA"} uses the null-hypothesis SE for the alpha term and the
alternative-hypothesis SE for the beta (power) term; \code{"NN"}/\code{"AA"} use
one SE for both.}

\item{quiet}{Logical. Suppress the printed summary?}
}
\value{
A list with the computed sample size or power, per-group
sample sizes, the anticipated probability table, and other design
parameters (mirroring the Stata command's stored results).
}
\description{
Implements the methods of White et al (2023), including their new ologit
method (which allows a non-proportional-odds anticipated effect via \code{pe},
is more accurate for large effects, and supports non-inferiority /
substantial-superiority trials via \code{margin}), and Whitehead's (1993)
original method for comparison.
}
\examples{
# FLU-IVIG trial (Davey et al 2019), reproducing White et al (2023) sec 4:
# default (new) ologit method
artcatr(pc = c(.018, .036, .156, .141, .39, .259), or = 1 / 1.77, power = .8,
       favourable = FALSE)

# Whitehead method, for comparison
artcatr(pc = c(.018, .036, .156, .141, .39, .259), or = 1 / 1.77, power = .8,
       favourable = FALSE, method = "whitehead")

# Non-inferiority trial (margin expressed as an odds ratio)
artcatr(pc = c(.010, .021, .099, .103, .384, .383), or = 1, margin = 1.33,
       power = .8, favourable = FALSE)
}
\references{
White IR, Marley-Zagar E, Morris TP, Parmar MKB, Royston P,
Babiker AG (2023). artcat: Sample-size calculation for an ordered
categorical outcome. Stata Journal 23(1):3-23.
https://doi.org/10.1177/1536867X231161934

Whitehead J (1993). Sample size calculations for ordered categorical
data. Statistics in Medicine 12:2257-2271.
}
