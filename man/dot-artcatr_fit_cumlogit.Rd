% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artcatr.R
\name{.artcatr_fit_cumlogit}
\alias{.artcatr_fit_cumlogit}
\title{Fit a weighted cumulative-logit model (internal)}
\usage{
.artcatr_fit_cumlogit(
  level_int,
  x,
  w,
  offset = rep(0, length(x)),
  estimate_b = TRUE
)
}
\arguments{
\item{level_int}{Integer vector of ordered outcome levels (coded
\code{1:levels_n}) for each observation}

\item{x}{Binary covariate value (here treatment group indicator)}

\item{w}{Numeric vector of case weights for each observation}

\item{offset}{Numeric vector of fixed offsets added to the linear
predictor for each observation (needed for non-inferiority margin)}

\item{estimate_b}{Logical; if \code{TRUE}, the coefficient \code{b} on \code{x} is
estimated, otherwise it is fixed at zero and only the cutpoints are
estimated}
}
\value{
A list with components:
b: Estimated coefficient on \code{x} (the log odds ratio), or \code{NA} if
\code{estimate_b} is \code{FALSE}
SE_b: Standard error of \code{b}, or \code{NA} if \code{estimate_b} is \code{FALSE}
zeta: Numeric vector of estimated cutpoints (length
\code{levels_n - 1})
}
\description{
Weighted maximum-likelihood fit of a cumulative-logit
model for an ordered outcome with a binary covariate \code{x}, allowing
arbitrary case weights and an optional fixed offset.
}
\examples{
The following example shows that
 .artcatr_fit_cumlogit() reproduces the coefficient, SE, and cutpoints
from a weighted proportional-odds model fit via MASS::polr(),
 Small discrepancy because of different optimizers.

levels_n <- 4
level_int <- rep(seq_len(levels_n), 2)
x <- c(rep(0, levels_n), rep(1, levels_n))
p1 <- c(0.3, 0.3, 0.2, 0.2)
p2 <- c(0.15, 0.25, 0.3, 0.3)
w <- c(p1 / 2, p2 / 2)

fit <- artcatr:::.artcatr_fit_cumlogit(level_int, x, w)
fit$b     # 0.7510
fit$SE_b  # 3.6513
fit$zeta  # -0.8948, 0.3871, 1.5188

df <- data.frame(
  y = factor(level_int, levels = seq_len(levels_n), ordered = TRUE),
  x = x
)
fit_polr <- MASS::polr(y ~ x, data = df, weights = w, Hess = TRUE)
coef(fit_polr)             # 0.7505
sqrt(vcov(fit_polr))[1, 1] # 3.6513
fit_polr$zeta              # -0.8953, 0.3867, 1.5184

}
